<% if object.errors.any? %>
  <div class="error_explanation">
    <h4><%= pluralize(object.errors.count, "error") %> found:</h>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
